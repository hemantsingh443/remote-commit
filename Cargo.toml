workspace = { members = [ "client", "daemon","git-actor", "mobile-core", "test-harness"] }
[package]
name = "remote-commit"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
